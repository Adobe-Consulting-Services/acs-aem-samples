/*global sampleModule*/

/*
 * The baseController is being assigned in the page template on the body element.
 * It is most useful when needing to assign values to scope which will be shared between all pages and components in the application.
 */
sampleModule.controller('baseController', ['$scope', function($scope) {

}]);
